"""Top-level application package for checkpoint-2 (the agent loop).

This package contains the FastAPI app, its dependency wiring, the tool
registry, the LLM service, and a from-scratch async agent loop. Each
sub-package is documented in its own ``__init__.py``.
"""
