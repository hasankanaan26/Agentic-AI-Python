"""Agent implementations.

This package will grow to host multiple agent backends. CP2 ships only
the from-scratch :func:`app.agents.raw_loop.run_agent` so engineers can
read the control flow without LangChain/LangGraph in the way.
"""
